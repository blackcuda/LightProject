#include "CLightCollection.h"

CLightCollection::CLightCollection(ILight* lightCollection[], uint32_t amount)
{
//    lightCollection[0]->setColour(40);

    std::cout << std::to_string(amount) << std::endl;
}
